export default {
  // hljs 主题
  codeStyle: 'atom-one-dark',
  toolbars: {
    // 粗体
    bold: true,
    // 斜体
    italic: true,
    // 标题
    header: true,
    // 下划线
    underline: true,
    // 中划线
    strikethrough: true,
    // 标记
    mark: true,
    // 上角标
    superscript: true,
    // 下角标
    subscript: true,
    // 引用
    quote: true,
    // 有序列表
    ol: true,
    // 无序列表
    ul: true,
    // 链接
    link: true,
    // 图片链接
    imagelink: true,
    // code
    code: true,
    // 表格
    table: true,
    // 全屏编辑
    fullscreen: true,
    // 沉浸式阅读
    readmodel: true,
    // 展示html源码
    htmlcode: true,
    // 帮助
    help: true,
    /* 1.3.5 */
    // 上一步
    undo: true,
    // 下一步
    redo: true,
    // 清空
    trash: true,
    // 保存（触发events中的save事件）
    save: true,
    /* 1.4.2 */
    // 导航目录
    navigation: true,
    /* 2.1.8 */
    // 左对齐
    alignleft: true,
    // 居中
    aligncenter: true,
    // 右对齐
    alignright: true,
    /* 2.2.1 */
    // 单双栏模式
    subfield: true,
    // 预览
    preview: true
  }
}
